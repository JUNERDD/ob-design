import type { DefaultProps } from '../../../index'
import type { ICardProps } from '../_types'

/**
 * ObCard - 参数默认值
 */
export default <DefaultProps<ICardProps>>{
  icon: '',
  title: '',
  text: '',
}
