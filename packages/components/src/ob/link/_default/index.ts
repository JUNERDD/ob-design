import type { DefaultProps } from '../../../index'
import { commonDefaultProps } from '../../../index'
import type { ILinkProps } from '../_types'

/**
 * ObLink - 参数默认值
 */
export default <DefaultProps<ILinkProps>> {
  ...commonDefaultProps,
}
