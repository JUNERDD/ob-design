import type { Theme } from '../_types'

/**
 * 圆角配置
 */
export const borderRadius: Theme['borderRadius'] = {
  max: '1.125rem',
  large: '1rem',
  normal: '0.75rem',
  small: '0.625rem',
  min: '0.375rem',
}
